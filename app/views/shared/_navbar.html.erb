<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to "Maino Blog", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to 'Home', root_path, class: "nav-link #{active_class(root_path)}" %>
        </li>
        <li class="nav-item">
          <%= link_to t('create_post'), new_post_path, class: "nav-link #{active_class(new_post_path)}" %>
        </li>
      </ul>
      <div class="nav-login">
        <% unless user_signed_in? %>
          <div class="nav-button-control d-flex justify-content-end">
            <%=  link_to t('sign_up'), new_user_registration_path, class: 'btn btn-secondary btn-md mx-3' %>
            <%= link_to t('log_in'), new_user_session_path, class: 'btn btn-primary btn-md mx-3' %>
          </div>
        <%  else %>
          <div class="nav-button-control d-flex justify-content-end">
            <%= link_to t('devise.registrations.edit.title', resource: current_user.name), edit_user_registration_path, class: 'btn btn-secondary btn-md mx-3' %>
            <%= button_to t('log_out'), destroy_user_session_path, method: :delete, class: 'btn btn-danger btn-md mx-3' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</nav>